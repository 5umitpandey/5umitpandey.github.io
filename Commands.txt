To test on local:
cd to path
use nvm 18
npm start


To push:
git checkout master
git add .
git commit -m "Updated this and that"
git push origin master / git push origin master --force


To deploy:
(stay on master, gh-pages will automatically use deployment branch)

Gatsby build
npm run deploy

DONE